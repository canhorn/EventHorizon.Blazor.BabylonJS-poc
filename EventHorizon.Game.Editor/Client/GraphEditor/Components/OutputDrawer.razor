@inherits BasePropertyDrawer

<FluentNumberField class="graph-node-field" Disabled="true" Label="@Property.Name" @bind-Value="value" />

@code {
    float? value
    {
        get => (float?)Property.GetValue(Instance);
        set { }
    }
}